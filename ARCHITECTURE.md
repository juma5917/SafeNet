# SafeNet - Visual Architecture Guide

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                      CLIENT BROWSER                              │
│                  http://localhost:3000                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────┐      ┌──────────────────┐                 │
│  │   React Pages    │      │  React Components│                 │
│  ├──────────────────┤      ├──────────────────┤                 │
│  │ • Home           │      │ • Navbar         │                 │
│  │ • Login          │      │ • Footer         │                 │
│  │ • Register       │      │ • Courses        │                 │
│  │                  │      │ • Resources      │                 │
│  │                  │      │ • ReportIncident │                 │
│  │                  │      │ • Support        │                 │
│  │                  │      │ • AdminDashboard │                 │
│  └──────────────────┘      └──────────────────┘                 │
│           │                         │                            │
│           └──────────────┬──────────┘                            │
│                          │                                       │
│            ┌─────────────▼──────────────┐                       │
│            │   Axios HTTP Client       │                       │
│            │  API Service Layer        │                       │
│            └─────────────┬──────────────┘                       │
│                          │                                       │
└──────────────────────────┼───────────────────────────────────────┘
                           │
         ┌─────────────────▼──────────────────┐
         │   Express.js API Server            │
         │   http://localhost:5000            │
         ├────────────────────────────────────┤
         │                                    │
         │  ┌────────────────────────────┐   │
         │  │   API Routes (7 groups)    │   │
         │  ├────────────────────────────┤   │
         │  │ • /auth                    │   │
         │  │ • /user                    │   │
         │  │ • /reports                 │   │
         │  │ • /resources               │   │
         │  │ • /courses                 │   │
         │  │ • /support                 │   │
         │  │ • /security-tips           │   │
         │  └────────────────────────────┘   │
         │            │                       │
         │  ┌─────────▼─────────────────┐    │
         │  │  Controllers (6 modules)  │    │
         │  ├───────────────────────────┤    │
         │  │ • authController          │    │
         │  │ • reportController        │    │
         │  │ • resourceController      │    │
         │  │ • courseController        │    │
         │  │ • supportController       │    │
         │  │ • securityTipController   │    │
         │  └─────────────────────────┘     │
         │            │                      │
         │  ┌─────────▼─────────────────┐   │
         │  │  Middleware               │   │
         │  ├───────────────────────────┤   │
         │  │ • JWT Authentication      │   │
         │  │ • Authorization           │   │
         │  │ • CORS                    │   │
         │  │ • Input Validation        │   │
         │  └───────────────────────────┘   │
         │                                   │
         └───────────────┬───────────────────┘
                         │
         ┌───────────────▼──────────────────┐
         │   MongoDB Database               │
         │   mongodb://localhost:27017      │
         ├────────────────────────────────┐ │
         │  Collections (7 total)          │ │
         ├────────────────────────────────┤ │
         │ • Users                         │ │
         │ • Reports                       │ │
         │ • Resources                     │ │
         │ • Courses                       │ │
         │ • UserProgress                  │ │
         │ • SecurityTips                  │ │
         │ • SupportResources              │ │
         └─────────────────────────────────┘ │
         │                                    │
         └────────────────────────────────────┘
```

---

## 📊 Data Flow Diagram

```
User Interaction
    ↓
React Component
    ↓
API Service (Axios)
    ↓
HTTP Request
    ↓
Express Route
    ↓
Middleware (Auth, Validation)
    ↓
Controller
    ↓
Model (Mongoose)
    ↓
MongoDB Database
    ↓
Response → JSON
    ↓
API Service
    ↓
React Component Updates State
    ↓
UI Re-renders
```

---

## 🔐 Authentication Flow

```
User Visits App
    ↓
Check localStorage for token
    ├─ Token exists?
    │  ├─ YES: Load user data, set isAuthenticated
    │  └─ NO: Redirect to Login/Home
    ↓
User submits credentials
    ↓
POST /auth/login
    ↓
Controller validates credentials
    ↓
Password hash verification
    ↓
Generate JWT token
    ↓
Return token + user data
    ↓
Store token in localStorage
    ↓
Axios adds token to headers
    ↓
Access protected routes
```

---

## 🔄 Request/Response Cycle

```
┌─────────────────────────────────────────────────────┐
│           CLIENT (React)                            │
│                                                     │
│  1. User clicks "Create Report"                    │
│  2. Form data collected                             │
│  3. Validation performed                            │
│  4. API call initiated                              │
│                                                     │
│     axios.post('/reports', reportData)             │
│                                                     │
└──────────────────┬──────────────────────────────────┘
                   │
                   │ POST /reports
                   │ Headers: { Authorization: Bearer token }
                   │ Body: { incident_type, severity, ... }
                   ▼
┌──────────────────────────────────────────────────────┐
│           SERVER (Express)                           │
│                                                      │
│  5. Route matches: POST /api/reports                │
│  6. Middleware processes:                           │
│     - CORS verification                             │
│     - JSON parsing                                  │
│     - JWT validation                                │
│     - Input validation                              │
│  7. Controller.createReport() called                │
│  8. Model validation                                │
│  9. Database insert: db.reports.insertOne()        │
│  10. Response generated                             │
│                                                      │
│      { success: true, report: {...} }              │
│                                                      │
└──────────────────┬──────────────────────────────────┘
                   │
                   │ 201 Created
                   │ Body: { report object }
                   ▼
┌──────────────────────────────────────────────────────┐
│           CLIENT (React)                            │
│                                                      │
│  11. API response received                          │
│  12. .then() or .catch() executed                   │
│  13. Component state updated                        │
│  14. Re-render with success message                 │
│  15. Form cleared or redirected                     │
│                                                      │
└──────────────────────────────────────────────────────┘
```

---

## 📱 User Journey

```
┌─────────────────────┐
│    Anonymous User   │
└──────────┬──────────┘
           │
           ▼
    ┌──────────────┐
    │ Landing Page │──── View Features
    └──────┬───────┘──── Explore Resources
           │         ──── See Courses
           │
        ┌──────────────────────┐
        │ Create Account / Login
        └──────┬───────────────┘
               │
               ▼
      ┌───────────────────┐
      │  Authenticated    │
      │  Regular User     │
      └────┬──────────────┘
           │
     ┌─────┼──────┬────────────┐
     │     │      │            │
     ▼     ▼      ▼            ▼
  Enroll Report  Browse     Access
  Course Incident Support   Security
     │     │      │    Tips
     │     │      │    │
     ▼     ▼      ▼    ▼
  Track  Report Admin  Learn
  Progress Status Dashboard
     │      │       │
     │      │       │
     └──────┼───────┘
            │
            ▼
    ┌─────────────────┐
    │ Get Certificate │
    └─────────────────┘
```

---

## 🗄️ Database Schema Relationships

```
┌─────────┐
│ Users   │
├─────────┤
│ _id     │(Primary Key)
│ name    │
│ email   │
│ role    │
└────┬────┘
     │
     ├─────────────────────┬──────────────┬──────────────┐
     │                     │              │              │
     │ 1:N relationship    │              │              │
     │                     │              │              │
     ▼                     ▼              ▼              ▼
┌─────────┐       ┌────────────┐  ┌────────────┐  ┌──────────┐
│ Reports │       │ Courses    │  │ Resources  │  │ Support  │
├─────────┤       ├────────────┤  ├────────────┤  │Resources │
│ _id     │       │ _id        │  │ _id        │  ├──────────┤
│user_id *│──────┤creator_id *│  │ N/A        │  │ _id      │
│incident │   1:N├────────────┤  ├────────────┤  └──────────┘
│severity │       │ lessons[]  │  │ title      │
│status   │       │ title      │  │ category   │  ┌────────────┐
└─────────┘       └────┬───────┘  │ views      │  │ Security   │
     │                 │          └────────────┘  │ Tips       │
     │                 │                          ├────────────┤
     │            1:N  │                          │ _id        │
     │                 ▼                          │ category   │
     │          ┌──────────────┐                  │ risk_level │
     │          │UserProgress  │                  └────────────┘
     │          ├──────────────┤
     │          │ _id          │
     │    ┌────┤user_id   *────┼──────┐
     │    │    │course_id *    │      │
     │    │    │progress       │      │
     │    │    │certificate    │      │
     │    │    └──────────────┘       │
     │    │                           │
     └────┴──────────────────────────┘
       M:N Relationship
```

---

## 🔑 Key Technology Interactions

```
Frontend                    Backend                  Database
─────────                   ───────                  ────────

React                    Express.js
  │                          │
  ├─ State Management        ├─ Route Handling
  ├─ Components              ├─ Middleware
  ├─ Routing                 ├─ Controllers
  │                          └─ Models
  │
  └─ Axios HTTP Client       JWT Token
      │                       │
      ├─ Interceptors ◄──────┤ Authentication
      ├─ Error Handling       │ Authorization
      └─ Request/Response ────┤
                              │
                         Mongoose ODM
                              │
                         MongoDB
                              │
                         ┌─────────────┐
                         │ Collections │
                         │   & Docs    │
                         └─────────────┘
```

---

## 📈 Scaling Diagram

```
Current Single Instance
┌─────────────────────────┐
│ Node.js + Express       │
│ MongoDB                 │
│ React App               │
└─────────────────────────┘

Scaled Production
┌──────────────────────────────────────┐
│        Load Balancer                 │
├──────────────────────────────────────┤
│                                      │
├─────────────┐  ┌──────────────────┐ │
│ Backend #1  │  │ Backend #2       │ │
│ Node.js     │  │ Node.js          │ │
└─────────────┘  └──────────────────┘ │
       │                  │           │
       └──────────┬───────┘           │
                  │                   │
           ┌──────▼──────┐            │
           │   MongoDB   │            │
           │   Replica   │            │
           │    Set      │            │
           └─────────────┘            │
                                      │
├─────────────────────────────────────┤
│  React App (CDN)                    │
│  Nginx Cache                        │
└──────────────────────────────────────┘
```

---

## 🎯 Feature Matrix

```
            ┌──────────────────────────────────────┐
            │         User Types                   │
├───────────┼──────────────────────────────────────┤
│ Features  │ Public │ User │ Educator │ Admin    │
├───────────┼───────┬────────┼────────┬──────────┤
│View Home  │  ✓    │  ✓   │  ✓    │  ✓      │
│View Courses│  ✓   │  ✓   │  ✓    │  ✓      │
│Enroll     │  ✗    │  ✓   │  ✓    │  ✓      │
│Report     │  ✗    │  ✓   │  ✓    │  ✓      │
│Create     │  ✗    │  ✗   │  ✓    │  ✓      │
│ Course    │       │      │       │         │
│Moderate   │  ✗    │  ✗   │  ✓    │  ✓      │
│View Stats │  ✗    │  ✗   │  ✗    │  ✓      │
│Manage     │  ✗    │  ✗   │  ✗    │  ✓      │
│ Users     │       │      │       │         │
└───────────┴───────┴───────┴───────┴─────────┘
```

---

## 📡 API Endpoint Structure

```
/api
├── /auth
│   ├── POST /register
│   └── POST /login
│
├── /user
│   ├── GET /profile
│   └── PUT /profile
│
├── /reports
│   ├── POST / (create)
│   ├── GET /user
│   ├── GET /all (admin)
│   ├── PUT /:id (admin)
│   └── GET /stats (admin)
│
├── /resources
│   ├── GET /
│   ├── GET /:id
│   ├── POST /
│   ├── PUT /:id
│   └── DELETE /:id
│
├── /courses
│   ├── GET /
│   ├── GET /:id
│   ├── POST /
│   ├── POST /:id/enroll
│   ├── POST /:id/lessons/:lesson_id/complete
│   └── GET /user/progress
│
├── /support
│   ├── GET /
│   ├── GET /:id
│   └── POST / (admin)
│
└── /security-tips
    ├── GET /
    ├── GET /:id
    └── POST /
```

---

## ✅ Ready to Deploy

The complete application is organized and ready for:
- Local development
- Docker deployment
- Cloud platforms
- Production scaling

See DEPLOYMENT.md for detailed instructions!
